
@{
    ViewBag.Title = "章节";
}

<div class="container">
    <div class="row">
        <div class="col-md-6">
            <h3>章节</h3>
            @Html.Action("SingleChapter", new { ViewBag.Id })
        </div>
        <div class="col-md-6">
            <h3>目录</h3>
            @Html.Action("DirectoryChapter", new { ViewBag.Id })
        </div>
    </div>
    <hr />
    <h3>子章节</h3>
    @Html.Action("ListChapter", new { ViewBag.Id })
</div>