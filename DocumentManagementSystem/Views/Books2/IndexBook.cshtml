
@{
    ViewBag.Title = "书籍";
}

<div class="container">
    <div class="row">
        <div class="col-md-6">
            <h3>书籍</h3>
            @Html.Action("SingleBook", new { bookId = ViewBag.Id })
        </div>
        <div class="col-md-6">
            <h3>目录</h3>
            @Html.Action("DirectoryBook", new { bookId = ViewBag.Id })
        </div>
    </div>
    <hr />
    <h3>子章节</h3>
    @Html.Action("ListChapter", new { ViewBag.Id })
</div>